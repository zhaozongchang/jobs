<div id="jobs">
  <div class="job">
    <h2><%= @job.title %></h2>
    <p><%= @job.description %></p>
    <p><%= @job.company %></p>
  </div>
</div>
<div class="text-center">
  <%= link_to("投交简历", new_job_resume_path(@job), class: "btn btn-default") %>
</div>
